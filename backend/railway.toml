[build]
builder = "nixpacks"

[deploy]
startCommand = "python -c \"import os; import uvicorn; uvicorn.run('server:app', host='0.0.0.0', port=int(os.environ.get('PORT', 8001)))\""
healthcheckPath = "/health"
